#pragma once

namespace utils::math::geometry::shape::sdf::return_type
	{
	class side;
	struct signed_distance;

	struct closest_point_with_signed_distance;
	struct direction_signed_distance;
	struct closest_point_with_distance;
	}